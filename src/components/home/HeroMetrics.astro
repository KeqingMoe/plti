<div class="hero__content">
  <div class="stat-grid">
    <article class="metric-card">
      <small>答题时间</small>
      <strong>8-12 分钟</strong>
      <p>分 6 页完成，每页 10 题，适合完整做完后直接分享结果。</p>
    </article>

    <article class="metric-card">
      <small>维度画像</small>
      <strong>6 维</strong>
      <p>类型、抽象、效应、控制、工程、范式探索各自独立计分。</p>
    </article>

    <article class="metric-card">
      <small>结果形式</small>
      <strong>1 + 2</strong>
      <p>主推 1 门语言，再给你 2 个备选结果和完整维度图谱。</p>
    </article>
  </div>
</div>

<style is:global>
  .stat-grid {
    display: grid;
    gap: 16px;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin-top: 18px;
  }

  .metric-card small {
    color: var(--muted);
    display: block;
    font-size: 0.84rem;
    letter-spacing: 0.08em;
    margin-bottom: 6px;
    text-transform: uppercase;
  }

  .metric-card strong {
    display: block;
    font-family: var(--display-font);
    font-size: 2rem;
  }

  .metric-card p {
    color: var(--ink-soft);
    margin: 10px 0 0;
  }

  @media (max-width: 900px) {
    .stat-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
